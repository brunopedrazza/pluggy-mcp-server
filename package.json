{
  "name": "pluggy-mcp-server",
  "version": "0.1.0",
  "description": "Read-only MCP server for personal financial data via Pluggy (Meu Pluggy / Connector 200), self-hosted.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "setup": "node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/setup-env.ts",
    "probe": "node --env-file-if-exists=.env --experimental-strip-types --disable-warning=ExperimentalWarning scripts/probe.ts",
    "dev": "node --env-file-if-exists=.env --experimental-strip-types --disable-warning=ExperimentalWarning --watch src/http.ts",
    "start": "node dist/http.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --experimental-strip-types --test test/**/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "pluggy-sdk": "^0.90.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
